# seq2seq-codegen
This project will explore how different recurrent neural network architectures perform on a text-to-code generation task, where natural language function descriptions (docstrings) are translated into Python source code.
